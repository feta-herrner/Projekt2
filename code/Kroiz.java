import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class Kroiz here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Kroiz extends Spiela
{
    public Kroiz () {
            GreenfootImage image = getImage () ;
            image.scale(image.getWidth() -943, image.getHeight() -943) ;
            setImage (image) ;
        }
    /**
     * Act - do whatever the Kroiz wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        //if (isTouching(felder2.actor))
        //{
            
        //}
    }
    
}
